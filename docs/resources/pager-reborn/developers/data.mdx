---
sidebar_position: 10
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Data Structures

This page documents the data structures used by PR exports and API payloads.

***

## Page Request (Server)

Used by:
- [`newPage` server export](exports/server.md#create-new-page)
- [`POST /page` API endpoint](api.mdx#create-a-new-page)

```lua
request.addresses
request.players
request.nature
request.body
```

<Tabs>
	<TabItem value="request.addresses" label="addresses">
		<p>`string[]`</p>
		<p>List of capcodes/addresses to send the page to.</p>
		<p>Example: <code>\{ "emg.fire.bc", "emg.ems.bc" \}</code></p>
	</TabItem>

	<TabItem value="request.players" label="players">
		<p>`number[]`</p>
		<p>Optional list of player server IDs to directly page.</p>
		<p>Example: <code>\{ 1, 2, 3 \}</code></p>
	</TabItem>

	<TabItem value="request.nature" label="nature">
		<p><code>PageNature</code> as lowercase <code>string</code> or enum <code>number</code></p>
		<p>The page type/nature.</p>
		<p>Example: <code>administrative</code> or <code>2</code></p>
	</TabItem>

	<TabItem value="request.body" label="body">
		<p>`string`</p>
		<p>Page message body.</p>
		<p>Must not be empty and must respect the configured max length.</p>
	</TabItem>
</Tabs>

***

## Local Page Request (Client)

Used by:
- [`newLocalPage` client export](exports/client.md#create-new-local-page)

```lua
request.nature
request.body
```

<Tabs>
	<TabItem value="localrequest.nature" label="nature">
		<p><code>PageNature</code> as lowercase <code>string</code> or enum <code>number</code></p>
		<p>The page type/nature.</p>
		<p>Example: <code>administrative</code> or <code>2</code></p>
	</TabItem>

	<TabItem value="localrequest.body" label="body">
		<p>`string`</p>
		<p>Page message body.</p>
	</TabItem>
</Tabs>

***

## Page Message

Used by:
- [`subscribeToNodes` server export](exports/server.md#subscribe-to-nodes)

```lua
page.read
page.nature
page.timestamp
page.body
```

<Tabs>
	<TabItem value="pagemessage.read" label="read">
		<p>`bool`</p>
		<p>Whether the message has been read.</p>
		<p>Will be omitted in server-side payloads.</p>
	</TabItem>

	<TabItem value="pagemessage.nature" label="nature">
		<p>`number` (`PageNature` enum value)</p>
		<p>The page type/nature as an enum value.</p>
		<p>Example: <code>0</code> (`emergency`)</p>
	</TabItem>

	<TabItem value="pagemessage.timestamp" label="timestamp">
		<p>`string`</p>
		<p>ISO 8601 timestamp string (round-trip format).</p>
		<p>Example: <code>2026-03-26T04:05:06.7890000+00:00</code></p>
	</TabItem>

	<TabItem value="pagemessage.body" label="body">
		<p>`string`</p>
		<p>The page message body text, in uppercase.</p>
	</TabItem>
</Tabs>

***

## Page Nature

Used by:
- [`newPage` server export](exports/server.md#create-new-page)
- [`newLocalPage` client export](exports/client.md#create-new-local-page)
- [`POST /page` API endpoint](api.mdx#create-a-new-page)

```lua
nature
```

- `emergency` or `0`
- `nonemergency` or `1`
- `administrative` or `2`

:::tip
Even if the nature types are renamed in the language file, they will still need to be written as above.
If you plan to rename the natures, we suggest using the enum values to avoid confusion.
:::
